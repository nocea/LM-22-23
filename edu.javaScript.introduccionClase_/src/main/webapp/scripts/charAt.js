//El método charAt saca la letra por indice,el 0 sería la posición número 1.
function charAt(texto,caracter){
	let text = texto;
	let letra = text.charAt(caracter);
	document.write(texto);
	document.write(letra);	
}